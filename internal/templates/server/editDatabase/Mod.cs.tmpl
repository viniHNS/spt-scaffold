using SPTarkov.DI.Annotations;
using SPTarkov.Server.Core.DI;
using SPTarkov.Server.Core.Extensions;
using SPTarkov.Server.Core.Models.Enums;
using SPTarkov.Server.Core.Models.Enums.Hideout;
using SPTarkov.Server.Core.Models.Spt.Mod;
using SPTarkov.Server.Core.Models.Utils;
using SPTarkov.Server.Core.Services;

namespace {{.ModName}};

public record ModMetadata : AbstractModMetadata
{
    public override string ModGuid { get; init; } = "com.{{.Author}}.{{.ModName}}";
    public override string Name { get; init; } = "{{.ModName}}";
    public override string Author { get; init; } = "{{.Author}}";
    public override List<string>? Contributors { get; init; }
    public override SemanticVersioning.Version Version { get; init; } = new("{{.Version}}");
    public override SemanticVersioning.Range SptVersion { get; init; } = new("{{.SptVersionRange}}");
    public override List<string>? Incompatibilities { get; init; }
    public override Dictionary<string, SemanticVersioning.Range>? ModDependencies { get; init; }
    public override string? Url { get; init; } = "{{.RepoURL}}";
    public override bool? IsBundleMod { get; init; }
    public override string License { get; init; } = "{{.License}}";
}

[Injectable(TypePriority = OnLoadOrder.PostDBModLoader + 1)]
public class {{.ModName}}Mod(
    ISptLogger<{{.ModName}}Mod> logger,
    DatabaseService databaseService) : IOnLoad
{
    public Task OnLoad()
    {
        EditGlobals();
        EditHideout();
        EditBots();
        logger.Success("{{.ModName}} finished editing the database!");
        return Task.CompletedTask;
    }

    private void EditGlobals()
    {
        // var globals = databaseService.GetGlobals();
        // globals.Configuration.SomeProperty = value;
        // TODO: Edit global game settings here
    }

    private void EditHideout()
    {
        // var hideout = databaseService.GetHideout();
        // hideout.Areas.First(a => a.Type == HideoutAreaType.Generator).Stages[1].ConstructionTime = 60;
        // TODO: Edit hideout areas/stages here
    }

    private void EditBots()
    {
        // var bots = databaseService.GetBots();
        // bots.Types["assault"].Difficulty.Normal.Core.CanGrenade = false;
        // TODO: Edit bot settings here
    }
}
